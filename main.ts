let x = 0
let y = 0
basic.forever(function () {
    x = input.acceleration(Dimension.X)
    y = input.acceleration(Dimension.X)
    if ((33 as any) > (32 as any)) {
        basic.showIcon(IconNames.Heart)
    }
})
